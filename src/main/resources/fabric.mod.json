{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${credits}"
  ],
  "contact": {
    "homepage": "${display_url}"
  },
  "license": "${mod_license}",
  "icon": "pack.png",
  "environment": "*",
  "depends": {
    "fabricloader": ">=0.18.4",
    "minecraft": "${minecraft_semver_range}"
  },
  "recommends": {
    "java": ">=${java_version_number}"
  }
}
